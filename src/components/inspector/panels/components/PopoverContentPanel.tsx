import React, { memo } from 'react'
import ChildrenControl from 'src/components/inspector/controls/ChildrenControl'

const PopoverContentPanel = () => <ChildrenControl />

export default memo(PopoverContentPanel)
